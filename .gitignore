node_modules/
dist/
data/
*.log
*.tar

# downloaded binaries (portable node, chrome, xray)
runtime/

# docker build: pre-downloaded linux binaries (ffmpeg/xray/chrome .deb)
docker/bin/

# root planning docs
/*.md

# tray panel: только собранный exe и артефакты компиляции, исходник — под git
panel/*.exe
panel/*.obj
panel/*.pdb

# секреты/ключи (на будущее — если появится шифрование)
.env
*.pem
*.key